# TASK-260212-1oraxm: vector-logo

## Description
Design minimalist vector SVG logo for Relux Works. Requirements:
- Clean, geometric, minimal — matches the dark tech aesthetic of the site
- Must work on dark and light backgrounds
- All fills/strokes use currentColor or CSS custom properties so palette can be swapped via CSS variables
- Multiple variants: full logo (icon + wordmark), icon only, wordmark only
- Sizes: works from 16px favicon to large hero placement
- Format: SVG with no raster dependencies, no fonts embedded (paths only)
- Concept direction: think Redux-like flow/cycle + 'R' + 'W' letterforms, agentic/modular feel
- Output files: public/logo.svg (full), public/logo-icon.svg (icon only), public/favicon.svg (optimized for small sizes)
- Integrate into Header.astro (replace text logo) and update favicon

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
