# TASK-260212-1xh3jn: header-hero

## Description
Header: sticky nav with logo (text 'Relux Works'), nav links (Services, Ecosystem, About, Community, Contact — scroll anchors), language switcher (EN/RU/ՀՅ/ქა), CTA button. Mobile: hamburger menu. Hero: full-viewport section, headline, subtitle, two CTA buttons (primary: contact, outline: ecosystem), subtle gradient background.

## Scope
(define task scope)

## Acceptance Criteria
- Header is sticky, transparent on top, solid on scroll
- Nav links scroll to sections
- Language switcher navigates between /en/ /ru/ /hy/ /ka/ preserving page
- Mobile hamburger menu works
- Hero section is full viewport height
- Two CTA buttons visible and linked
- All text from locale YAML
